class Core
{
  String id;
  String installed_version;
  String latest_version;
  String name;
  Core({required this.id, required this.installed_version, required this.latest_version, required this.name});
}